{
  "name": "rule-sets",
  "version": "0.0.1",
  "description": "rule set that allows to define dependencies and exclusiveness between packages",
  "type": "commonjs",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "nyc --reporter=text mocha -r ts-node/register __tests__/**/*.spec.ts",
    "build": "tsc --watch"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.3.4",
    "@types/mocha": "^10.0.1",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "nyc": "^15.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
